import { PasswordResetResponse } from './password-reset-response';

describe('PasswordResetResponse', () => {
  it('should create an instance', () => {
    expect(new PasswordResetResponse()).toBeTruthy();
  });
});
