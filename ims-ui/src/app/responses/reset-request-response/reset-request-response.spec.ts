import { ResetRequestResponse } from './reset-request-response';

describe('ResetRequestResponse', () => {
  it('should create an instance', () => {
    expect(new ResetRequestResponse()).toBeTruthy();
  });
});
